type RasterDefinition {
    id: ID!
    outputGranuleExtentFlag: Boolean!
    outputSamplingGridType: String!
    rasterResolution: GridType!
    rasterResolution: Int!
    utmZoneAdjust: Int!
    mgrsBandAdjust: Int!
}
