mutation ($rasterDefinitionID: ID!) {
	createL2RasterProduct(definition: $rasterDefinitionID) {
		id
		definition {
            id
        }
        status {
            id
			timestamp
            state
            reason
        }
    }
}